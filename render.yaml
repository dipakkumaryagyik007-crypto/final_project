services:
  web:
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn MyProject.wsgi:application"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: MyProject.settings

  workingDir: MyProject
  preStartCommand: "ls -R"
